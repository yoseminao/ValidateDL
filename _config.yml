# Build settings
# Gems
gems: 
- jekyll-paginate

# url is the full website URL
# baseurl is the website's URL without the hostname
# If you are hosting it on Github for some project with name 'projectname', then the url and baseurl should look like:
#url: "http://username.github.io/projectname"
#baseurl: "/projectname"
url: ""
enforce_ssl: ""
baseurl: "/ValidateDL"

# Title of website
title: LTO Driver's License Validator

#Default keywords (used if no keywords are specified on a page basis)
keywords: LTO, Driver, License

# Short description of your site
desc: Philippines LTO Driver's License Validator

 
# --- Footer options --- #

# If the values are empty, they are ignored 
profile:
  name: Jose Minao
  email: joseminao@protonmail.com
  github: yoseminao

# --- Misc --- #

# Google Analytics Settings
google_analytics: "Your-GA-Id"

# Set these options as you need (For more information, check Jekyll's site)
timezone: "Asia/Manila"
markdown: kramdown
#highlighter: rouge
kramdown:
  input: GFM
  syntax_highlighter: rouge
permalink: /blog/:title
paginate: 5

# Default YAML values (more information on Jekyll's site)
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      comments: true  # add comments to all blog posts
  -
    scope:
      path: "" # all files
    values:
      layout: "default"
      show-avatar: true

# Exclude these files from production site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME
